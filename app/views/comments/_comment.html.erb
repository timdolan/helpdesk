<div id="ticket_comment" data-time="<%= comment.created_at.to_i %>">
<table >
  <tr>
    <td>
      <h1><%= "#{comment.created_at} | #{comment.user.first_name} #{comment.user.last_name}" %></h1><p><%= comment.body %></p></div>
    </td>
  </tr>
</table>